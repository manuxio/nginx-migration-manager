# local secrets / per-machine config
.env

# bind-mounted runtime data (nginx config, certs, app data)
/data/

# node
node_modules/
npm-debug.log*
yarn-error.log*

# build output (produced inside the Docker image build)
app/web/dist/
app/server/public/

# os cruft
.DS_Store
Thumbs.db
